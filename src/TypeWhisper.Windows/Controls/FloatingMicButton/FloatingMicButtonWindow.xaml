<Window x:Class="TypeWhisper.Windows.Controls.FloatingMicButton.FloatingMicButtonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ResizeMode="NoResize"
        SizeToContent="WidthAndHeight">
    <Grid Margin="26">
        <!-- Sonar rings rendered behind the button, centered via code-behind -->
        <Canvas x:Name="SonarCanvas"
                IsHitTestVisible="False"/>
        <Border x:Name="ButtonBorder"
                CornerRadius="100"
                Cursor="Hand"
                RenderTransformOrigin="0.5,0.5">
            <Border.RenderTransform>
                <ScaleTransform x:Name="PulseScale"/>
            </Border.RenderTransform>
            <Border.Background>
                <SolidColorBrush x:Name="ButtonBackground" Color="#2D2D42"/>
            </Border.Background>
            <Border.Effect>
                <DropShadowEffect BlurRadius="14" ShadowDepth="3" Opacity="0.55" Color="Black"/>
            </Border.Effect>
            <Grid>
                <!-- Press-ripple, clipped to circle in code-behind -->
                <Canvas x:Name="RippleCanvas" IsHitTestVisible="False"/>
                <TextBlock x:Name="ButtonIcon"
                           Text="&#xE720;"
                           FontFamily="Segoe MDL2 Assets"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="#AADDDDDD"/>
            </Grid>
        </Border>
    </Grid>
</Window>
